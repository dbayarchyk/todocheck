import { defineMessages } from "react-intl";

const scope = "pages.NewTask";

export default defineMessages({
  backLink: {
    defaultMessage: "Back to the tasks",
    id: `${scope}.backLink`,
  },
  headline: {
    defaultMessage: "New Task",
    id: `${scope}.headline`,
  },
});
