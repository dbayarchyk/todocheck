import { defineMessages } from "react-intl";

const scope = "pages.Settings";

export default defineMessages({
  backLink: {
    defaultMessage: "Back to the tasks",
    id: `${scope}.backLink`
  },
  headline: {
    defaultMessage: "Settings",
    id: `${scope}.headline`
  },
  language: {
    defaultMessage: "Language",
    id: `${scope}.language`
  }
});
