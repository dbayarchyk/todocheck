import { defineMessages } from "react-intl";

const scope = "components.DatePicker";

export default defineMessages({
  addDateTime: {
    defaultMessage: "Add date/time",
    id: `${scope}.addDatetime`,
  },
  removeDate: {
    defaultMessage: "Remove the date",
    id: `${scope}.removeDate`,
  },
  time: {
    defaultMessage: "Time",
    id: `${scope}.time`,
  },
  cancel: {
    defaultMessage: "Cancel",
    id: `${scope}.cancel`,
  },
  save: {
    defaultMessage: "Save",
    id: `${scope}.save`,
  },
});
